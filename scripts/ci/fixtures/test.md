# Native highlighting

A **bold** word, *emphasis*, `inline code`, and [a link](https://example.com).

```bash
printf '%s\n' "$HOME"
```

```php
<?php echo $name;
```

```javascript
const message = `Hello ${name}`;
```

```blade
@if ($user)
  <strong>{{ $user->name }}</strong>
@endif
```
