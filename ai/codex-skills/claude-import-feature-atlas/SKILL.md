---
name: claude-import-feature-atlas
description: Discover, document, audit, refresh, or report on feature inventories stored under .claude/feature-atlas.
---

Read the requested maintained workflow completely: `commands/feature-atlas.md`, `commands/feature-atlas-subsystem.md`, or `commands/feature-atlas-report.md` under `~/dotfiles/ai/`. When a specialized phase is needed, read the matching `feature-atlas-*.md` under `~/dotfiles/ai/agents/`. Keep `.claude/feature-atlas/` as the durable cross-agent format. Delegation is optional unless the user explicitly requests it.
